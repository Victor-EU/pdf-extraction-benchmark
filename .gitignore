# ── Secrets ─────────────────────────────────────────────
.env
*.key
.mcp_publisher_token

# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.venv-docling/
.venv-liteparse/
.venv-mistral/
.venv-*/

# ── OS / editor ─────────────────────────────────────────
.DS_Store
*.swp

# ── Corpus data & full reproductions (NOT published) ────
# The benchmark runs on source PDFs whose full content is
# reproduced by the transcriptions, renders, and per-vendor
# reconstructions below. These are intentionally excluded —
# the insurance corpus is personal / identifying data.
Data/
ground_truth/
results/*
!results/*.md
# old finance-corpus vendor reproductions (full 599-page extractions) — data, not published
archive/old_corpus_artifacts/
